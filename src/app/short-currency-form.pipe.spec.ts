import { ShortCurrencyFormPipe } from './short-currency-form.pipe';

describe('ShortCurrencyFormPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortCurrencyFormPipe();
    expect(pipe).toBeTruthy();
  });
});
