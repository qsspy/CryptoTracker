import { CurrencyIntervalHistory } from './currency-interval-history';

describe('CurrencyIntervalHistory', () => {
  it('should create an instance', () => {
    expect(new CurrencyIntervalHistory()).toBeTruthy();
  });
});
