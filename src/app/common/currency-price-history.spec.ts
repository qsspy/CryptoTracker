import { CurrencyPriceHistory } from './currency-price-history';

describe('CurrencyPriceHistory', () => {
  it('should create an instance', () => {
    expect(new CurrencyPriceHistory()).toBeTruthy();
  });
});
